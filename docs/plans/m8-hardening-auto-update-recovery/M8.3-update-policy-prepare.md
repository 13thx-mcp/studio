# M8.3 — Update Checks, Notify-Only and Auto-Prepare

**Status:** VERIFIED / CLOSED — qualified at Studio `412ebe1`; see [M8.3 qualification](M8.3-QUALIFICATION.md).

## Objective

Enable bounded unattended release observation and verified staging without any automatic activation.

## Primary source areas

- `src/update/inventory.rs`
- component `prepare()` managers
- `src/update/staging.rs`
- AutomationController policy adapters
- M6 update-check/history projection
- Updates UI state

## Sequence

1. Add startup/periodic check scheduling.
2. Enforce one check batch at a time.
3. Implement target selection: explicit desired > trusted latest stable.
4. Implement notify-only state/event/history.
5. Implement auto-prepare dispatcher to existing managers.
6. Record current-process prepared references in AutomationState.
7. Inventory existing ready staging on startup.
8. Add TTL/aggregate budget and safe cleanup eligibility.
9. Observe source Git hygiene without modifying source.
10. Add provider outage/backoff/circuit tests.

## Safety rules

- prepare cannot stop/restart/replace runtime;
- previous-process generic/Gateway/Fleet prepared transactions are not activation authority;
- dirty source is surfaced as future activation blocker only;
- unsupported/pre-release/downgrade/same-version targets fail policy selection;
- staging cleanup cannot touch component recovery journals or active releases.

## Exit criteria — PASS FOR M8.3 SCOPE

R25–R31 PASS. R32/R33 source-hygiene observation is qualified; their activation-enforcement portions remain explicitly assigned to M8.4. Auto-prepare is safe to enable independently of M8.4, while `auto-update-safe` remains prepare-only.
