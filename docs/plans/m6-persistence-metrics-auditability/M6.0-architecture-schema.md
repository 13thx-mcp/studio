# M6.0 — Architecture/common contracts and schema freeze

**Status:** **VERIFIED / CLOSED** in the final M6 disposition. This file preserves the implementation plan/exit contract; execution evidence is in [milestone status](../../milestone-6-status.md) and [M6.8 closure](M6.8-verification-closure.md).

## Objective
Convert this proposed plan into a reviewed, internally consistent contract before adding a database dependency.

## Dependencies and entry criteria
Current source audit; no implementation dependency.
Read [COMMON](COMMON.md), [SOURCE-AUDIT](SOURCE-AUDIT.md), [SCHEMA](SCHEMA.md), [EVENTS](EVENTS-AND-INTEGRATION.md) and the package-specific companion contracts before editing. Recheck source drift from `6a6122abb41591f22874fb439eb8427edd649e53` and do not overwrite unrelated work. Requirements: R01.

## Source files and modules affected
Existing ROADMAP.md, docs/architecture.md, docs/threat-model.md and ADR 0004–0017; this planning directory; proposed ADR 0018–0025; docs/milestone-6-status.md. No src/, Cargo or web runtime changes in this package.

## Schema changes
Freeze complete candidate schema v1 in SCHEMA.md, immutable migration naming, event/payload version 1, metric definition 1, application_id and logical versus physical foreign-key conventions.

## Public and internal contracts
COMMON invariants M6-I01–M6-I16 are mandatory. No raw SQL connection crosses storage ownership; producers send typed bounded observations and receive effect/audit-correlated receipts. Keep current owner DTOs/actions distinct from new read-only history DTOs. Preserve file formats and M5 recovery/ownership unless an explicitly reviewed prerequisite in this package fixes a demonstrated source gap.

## Implementation sequence
1. Check branch/HEAD/worktree against SOURCE-AUDIT.md. Review changed source anchors, not the whole architecture from scratch. Preserve unrelated work and record any base drift.
2. Resolve/freeze ADR-PLAN.md choices, including safe-stop exception, admission/terminal headers, writer capacity and crash-window limits. Record acceptance dates; do not label current design accepted merely because it exists.
3. Review P-01 generic MCP apply and P-02 registry serialization. Confirm the exact M6.2 test-first fix points and no new lifecycle authority.
4. Validate candidate DDL and schema dependency/indexes. Review every event name and payload against an actual owner proof. Distinguish terminal Completed from verification that precedes cleanup.
5. Run the adversarial review checklist and record explicit residual limits. Review endpoint/metric names and default bounds with the source owners.
6. Update architecture/threat model with links to proposed/accepted contracts and leave M5 publication status unchanged.

## Failure semantics
Any unresolved competing authority, unsafe failure path, unbounded query/growth or missing M6.1 startup contract blocks schema freeze. Dependency build evidence may remain a named M6.1 gate, but the architecture must not be left as several optional incompatible designs.

## Permanent tests
Document-relative-link validation; candidate DDL creation/FK/unique/rollback checks; immutable event-to-owner mapping review. These are design checks, not Rust production tests.
All new names above are **planned tests**, not existing passing tests. Map final module-qualified names into [M6-VERIFICATION-MATRIX](M6-VERIFICATION-MATRIX.md); assert executed counts, not substring-command success.

## Security tests
Review secret/path-bearing source structs against allowlist DTOs; review same-user tamper limitation and symlink trust boundary; verify M7/M8 exclusions.

## Operator / real-runtime verification
Read runtime layout metadata only. Confirm both legacy-flat and versioned Studio data locations without starting/stopping installed components.
Never use the installed runtime as a destructive fixture. Record exact provenance, commands, outcomes and sanitized evidence. A fixture PASS does not imply native/publication qualification.

## Documentation and ADR updates
Accept or amend ADR 0018–0025 with source references. Record schema fixture SHA-256 and requirement matrix version.

## Exact exit criteria
All architecture contracts accepted with no blocking design ambiguity; M6.1 has exact files, DDL, dependency candidate, startup sequencing, failure behavior and test obligations. No production code or version changed.
Package status moves from NOT STARTED → IMPLEMENTING → VERIFIED only when its permanent tests and required real-runtime evidence support that state. Partial completion and blocked evidence remain explicit.

## Explicit non-goals
No SQLite integration, live actions, release unblock, production test PASS, version bump or tag.
