# M6.8 — Full qualification, migration/release gates and closure

**Status:** VERIFIED / CLOSED. This document remains the normative exit contract; final execution disposition is recorded below.

## Objective
Prove end-to-end persistence and explainable lineage through real restart/update/failure scenarios, with clean provenance and honest release status.

## Dependencies and entry criteria
M6.0–M6.7 implementation exits satisfied and evidence mapped; M5 publication gate remains separate.
Read [COMMON](COMMON.md), [SOURCE-AUDIT](SOURCE-AUDIT.md), [SCHEMA](SCHEMA.md), [EVENTS](EVENTS-AND-INTEGRATION.md) and the package-specific companion contracts before editing. Recheck source drift from `6a6122abb41591f22874fb439eb8427edd649e53` and do not overwrite unrelated work. Requirements: R41, R42, R43, R44.

## Source files and modules affected
Add scripts/verify-m6-history.py and docs qualification/closure evidence mapping; update docs/milestone-6-status.md, ROADMAP.md and operator docs only with verified facts. Existing M5 runner and native package scripts remain reference gates.

## Schema changes
No discretionary schema redesign at closure; any found schema correction reopens the relevant package and needs a numbered migration/fixture.

## Public and internal contracts
COMMON invariants M6-I01–M6-I16 are mandatory. No raw SQL connection crosses storage ownership; producers send typed bounded observations and receive effect/audit-correlated receipts. Keep current owner DTOs/actions distinct from new read-only history DTOs. Preserve file formats and M5 recovery/ownership unless an explicitly reviewed prerequisite in this package fixes a demonstrated source gap.

## Implementation sequence
1. Implement a synchronous foreground qualification runner with distinct profiles foundation, regressions, security, web, restart, runtime-only, retention, native and full. Required artifacts/tests missing or zero-execution count are failures, not PASS/SKIP.
2. Capture Studio/Fleet HEAD+worktree, lockfile/toolchain hashes, SQLite version/source ID, OS/arch and input artifact digests before/after. Use disposable output/runtime dirs, fail-fast per gate with retained logs, process-group cleanup and summary/manifest.
3. Run Rust fmt/check/Clippy/test/debug+release build and frontend install/lint/typecheck/test/build; dependency audits use timestamped advisory sources. Run explicit M5 regression list, including ignored audit tests where applicable, with nonzero counts.
4. Run real clean restart, observer crash, DB lock/full/corrupt/unsafe path, source journal separation, concurrent operations, transaction rollback and long-history/retention tests. Do not substitute a mocked backend for native ownership/recovery evidence.
5. Qualify source-less legacy/versioned layouts, self-update across two real processes, backup/restore, candidate schema upgrade and binary rollback on the required darwin-arm64 native target. Rosetta/cross-build does not substitute for native arm64.
6. Run an independent final source/design review for authority, audit phase, cancellation, retention and secret leaks. Reopen packages for findings and rerun affected plus full gates.
7. Close implementation only with complete R01–R44/M5/gate evidence. Keep M5 publication-qualified successor gate BLOCKED until its independent native/public-artifact proofs actually clear. Version/tag/push/publication require separate authorized execution, not an implication of this plan.

## Failure semantics
Any failed/blocked/not-run mandatory gate prevents full qualification/closure. Preserve partial evidence and exact failure logs. No ignored flaky test, fake PASS, skipped native architecture or changed source after evidence can support closure.

## Permanent tests
All tests in M6-VERIFICATION-MATRIX.md plus original required M5/F1–F8 audit tests; permanent binary_rollback_never_downgrades_history_schema and history_housekeeping_never_invokes_runtime_policy. Repeat full matrix after final code changes.
All new names above are **planned tests**, not existing passing tests. Map final module-qualified names into [M6-VERIFICATION-MATRIX](M6-VERIFICATION-MATRIX.md); assert executed counts, not substring-command success.

## Security tests
Dependency audit, secret-redaction corpus, file/symlink/SQL/origin/size/corruption attacks and installed artifact provenance. Audit evidence itself must omit secrets and arbitrary environment capture.

## Operator / real-runtime verification
Foreground runner creates timestamped local evidence and a readable summary with PASS/FAIL/BLOCKED/NOT RUN per gate, exact commands and executed test counts. Public artifact/bootstrap/update proof is a separate release gate; record no fresh public endpoint claim without actually verifying it.
Never use the installed runtime as a destructive fixture. Record exact provenance, commands, outcomes and sanitized evidence. A fixture PASS does not imply native/publication qualification.

## Documentation and ADR updates
Milestone status, verification matrix and independent re-review/closure report; ADR implementation links; migration/backup/downgrade operator runbook. Never overwrite historical M5 evidence claims with unrelated M6 results.

## Exact exit criteria
All implementation/qualification gates and clean-source provenance are verified, no unresolved review findings, and recorded history explains current observed artifact/config lineage or explicit genuine gap/retention boundaries. Publication status is separately and truthfully reported.
Package status moves from NOT STARTED → IMPLEMENTING → VERIFIED only when its permanent tests and required real-runtime evidence support that state. Partial completion and blocked evidence remain explicit.



## 2026-09-21 execution disposition

`scripts/verify-m6-history.py` is implemented with the planned profiles, bounded foreground execution, per-command logs, exact test-count validation, before/after Git/toolchain/lock provenance, summary and hashed manifest.

Full clean-source qualification passed on native arm64 with independently qualified M5 publication evidence. Q13 native coverage, Q14 provenance, Q10–Q12 current-host scenarios, and Q16 publication evidence all passed. No mandatory command was failed, blocked, or skipped.

## Explicit non-goals
No M5 publication-blocker repair hidden in M6 scope; no automatic M7 policy; no publication/version/tag/push without explicit authorization; no closure based solely on docs or mocks.
